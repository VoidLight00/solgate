# solgate — Failure Log

> 실패할 때마다 1행. 같은 실패 재발 방지용 게이트를 추가하고 여기 기록.

| date | id | symptom | root cause | fix | gate added |
|---|---|---|---|---|---|
| 2026-07-10 | SG-001 | compact 게이트 PASS인데 stats.degraded=13 — 요약이 전부 절단 마커로 강등된 채 통과 | ① gpt-5.6-luna가 cli-proxy-api 7.2.54에서 `auth_unavailable`(sol·terra는 정상 — luna 카탈로그 override_header 특수처리 의심) ② 강등 마커를 캐시에 저장해 실패가 영구 오염 ③ 게이트가 degraded를 안 봄 | SUMMARY_MODEL 기본값 terra로 전환 + 강등 마커 캐시 저장 금지 + 오염 캐시 purge | e2e_compact_gate.sh에 degraded 증가 = FAIL 추가 |
| 2026-07-12 | SG-002 | -1m 세션(~400k)이 압축 후에도 업스트림 400 `context_too_large` — Claude Code에 API Error 그대로 노출, 세션 정지 (07:03:14, compacted:true·80.6s) | ① `enforceCeiling`이 recent에 user 경계가 없으면 `break`로 천장 초과 페이로드를 그대로 전송(SR5 위반) — 자율 실행 세션은 user 턴 희소 ② 추정치(other/3.6)가 코드·JSON 밀도 구간에서 과소 — 330k/372k 마진 11%를 잠식 ③ 에러 경로 logLine이 meta 누락이라 로그만으로 진단 불가 | ① fail-closed: user 경계 소진 후 큰 메시지 본문 비율 절단(FLOOR 2k tok, CJK 보존) + 최후 앞절단 ② 400 context_too_large 시 천장 0.8배로 1회 재압축-재시도(ctxRetries stat) ③ 에러 logLine에 meta 스프레드 | unit.test.mjs SR5 fail-closed 2건 + ctxretry.e2e.test.mjs(첫 본문요청 400 mock → 재시도 200) — unit_gate.sh에 편입 |
| 2026-07-16 | SG-003 | -1m 세션이 압축 후 `finalEst 223,916`(천장 330k 아래)인데 업스트림 400 `context_too_large` — SG-002가 넣은 재시도가 돌았는데도 그대로 세션 정지. 로그상 재시도 전후 223,916 → 223,756 (160토큰) | SG-002의 재압축이 **명목 천장 기준**(`HARD_CEILING*0.8` = 264k)으로 축소 — 실제 보낸 값 224k는 이미 264k 아래라 재압축이 **no-op**. 같은 body 재전송 → 같은 400 → `ctxRetried=true`로 포기. 폐루프가 구조적으로 안 닫힘. 기존 `ctxretry.e2e.test.mjs` mock이 **횟수 기반**(첫 1회만 400)이라 no-op 재시도도 2회차 200을 받아 통과 — 게이트가 fail-open이라 07-12부터 초록인 채 방치 | 축소 기준을 명목 천장 → **직전 `meta.finalEst`**(`sent * 0.7`)로 변경 + 1회 → 최대 3회 점진 축소(0.7³≈34%) + **진전 없으면(`finalEst >= sent`) 재시도 중단하고 에러 노출**(no-op 루프 금지). 절대 바닥값은 두지 않음 — MAX_RETRIES가 하한을 구조적으로 보장 | `tests/ctxwindow.e2e.test.mjs` — mock이 **실제 크기**(REAL_WINDOW)로 거부. no-op 재시도면 400이 반복돼 FAIL. 구버전 코드에서 RED(exit 1) / 수정본 GREEN(exit 0) 양방향 실증 후 unit_gate 편입 |
| 2026-07-10 | SG-001b | (SG-001 근인 확정) luna `auth_unavailable`은 VibeProxy 내장 엔진 7.2.54의 구버전 버그 — 카탈로그의 luna `config.override_header`(07-09 추가)를 처리 못함. 7.2.58(커밋 26d45fd)에서 수정 확인(격리 인스턴스 :8390 실측 — auth 통과, 실 업스트림 응답 도달) | VibeProxy 앱 바이너리는 수정하지 않고(서명 무결성) CLIProxyAPI 7.2.58 사이드카 `com.voidlight.cpap-sidecar` :8331 상주 + CCR provider `cpapside`로 luna만 라우팅. VibeProxy 엔진 7.2.58+ 내장 시 사이드카 제거 | 배선은 wiring 정신으로 custom-router SIDECAR_MODELS 주석에 회수 조건 명시. solgate 요약 모델은 terra 유지(solgate upstream=8317 구엔진이라 luna 불가) |
| 2026-07-21 | PM-20260721-01 | vgpt/vgpt1m 전 라우트 500 `dial tcp: lookup chatgpt.com: no such host` — voidnews 델타 executor run 중단 | mDNSResponder 캐시에 chatgpt.com A레코드만 소실(AAAA-only 잔존) + utun0 링크로컬 IPv6 기본경로 블랙홀 — getaddrinfo 소비자(curl·Go solgate) 전멸, dig/nslookup은 정상이라 진단 혼선 | sudo 불가 상황에서 `dns-sd -G v4 chatgpt.com` 무권한 강제 A재질의로 캐시 복구 → curl 403·vgpt1m PONG 실측. 죽은 v6 경로는 잔존(workaround) | 게이트 없음 — 세션 모니터에 `API Error: 5xx|no such host` 즉시 감지 배선, 상세 docs/postmortems/PM-20260721-01-*.md |
